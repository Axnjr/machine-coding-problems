I need to build a `BowlingAlley`! In which `Players` play `Game` in turns or `Frames` and at the end of the game i need to show the `Score`.

# LLD was designing a portfolio tracker with functionalities to buy/sell stock and show total returns etc